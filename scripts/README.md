# Legacy Scripts (Deprecated)

These scripts are the old implementation and are kept for reference only.

**⚠️  DO NOT USE THESE SCRIPTS**

Use the new CLI instead:

```bash
repowiki test       # Instead of: python test_setup.py
repowiki index      # Instead of: python 01_index_repository.py
repowiki generate   # Instead of: python 02_hierarchical_wiki_generator.py
repowiki all        # Instead of: python 00_run_all.py
```

See `MIGRATION.md` for complete migration guide.
